# Vite

```ts [vite.config.ts]
import { defineConfig } from "vite";
import compdi from "compdi/plugin/vite";

export default defineConfig({
  plugins: [compdi()],
});
```

The same configuration applies to Vite-based development and production builds. Import macros from `compdi/macros` in application source files.
