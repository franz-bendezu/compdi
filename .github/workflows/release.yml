name: Release & Publish

on:
  workflow_dispatch:
    inputs:
      version-bump:
        description: 'Version bump type (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
      dry-run:
        description: 'Perform a dry run without publishing'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [24.x]
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: 'https://registry.npmjs.org'
          cache: npm
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run build
        run: npm run build
      
      - name: Run typecheck
        run: npm run typecheck
      
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Resolve release version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "value=${VERSION}" >> "$GITHUB_OUTPUT"
      
      - name: Validate package integrity
        run: |
          # Verify that key distribution files exist
          test -f packages/core/dist/index.mjs || exit 1
          test -f packages/core/dist/index.d.mts || exit 1
          test -f packages/compdi/dist/index.mjs || exit 1
          test -f packages/compdi/dist/index.d.mts || exit 1
          echo "✓ All distribution files verified"
      
      - name: Publish to npm (Dry Run)
        if: ${{ inputs.dry-run == true }}
        run: |
          echo "📋 Dry Run Mode: Simulating publish..."
          echo "Would publish the following packages:"
          echo "  - @compdi/core (public)"
          echo "  - compdi (public)"
          echo "  - @compdi/shared (private, skipped)"
          echo "  - unplugin-compdi (private, skipped)"
      
      - name: Publish to npm
        if: ${{ inputs.dry-run == false }}
        run: |
          # Trusted publishing with OIDC provenance.
          npm publish --workspace packages/core --access public --provenance
          npm publish --workspace packages/compdi --access public --provenance
          echo "✓ Packages published successfully"
      
      - name: Create Git tag
        if: ${{ inputs.dry-run == false }}
        run: |
          VERSION="${{ steps.version.outputs.value }}"
          git tag -a "v${VERSION}" -m "Release v${VERSION}"
          git push origin "v${VERSION}"
      
      - name: Create GitHub Release
        if: ${{ inputs.dry-run == false }}
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.version.outputs.value }}
          name: Release v${{ steps.version.outputs.value }}
          body: |
            ## Published Packages

            ### Public
            - @compdi/core@${{ steps.version.outputs.value }} - Type-safe DI macros
            - compdi@${{ steps.version.outputs.value }} - Unified public API

            ### Private (not published)
            - @compdi/shared@${{ steps.version.outputs.value }} - Internal utilities
            - unplugin-compdi@${{ steps.version.outputs.value }} - Build plugin

            ## Links
            - npm: https://www.npmjs.com/package/compdi
            - scoped: https://www.npmjs.com/package/@compdi/core
          draft: false
          prerelease: false

  verify:
    needs: release
    if: ${{ inputs.dry-run == false }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Verify npm publication
        run: |
          VERSION=$(npm view compdi version)
          echo "✓ compdi@${VERSION} verified on npm"
          
          CORE_VERSION=$(npm view @compdi/core version)
          echo "✓ @compdi/core@${CORE_VERSION} verified on npm"
